- title "Tabs"

%table
  %tr
    %th ID
    %th Name
    %th Cashier
    %th Drawer
    %th Closed At
    %th Type
    %th Purchases
    %th Transactions
  - for tab in @tabs
    %tr
      %th= h tab.id
      %td= h tab.name
      %td= h tab.user.username
      %th= link_to tab.drawer_id, drawer_path(tab.drawer_id)
      - if tab.open?
        %td= "Open"
      - else
        %td= h tab.closed_at
      %td= h tab.type
      %td= link_to tab.purchases.count, tab_purchases_path(tab)
      %td= link_to tab.transactions.count, tab_transactions_path(tab)
      %td= link_to 'Show', tab
      %td= link_to 'Edit', edit_tab_path(tab)
      %td= link_to 'Destroy', tab, :confirm => 'Are you sure?', :method => :delete

%p= link_to "New Tab", new_tab_path
